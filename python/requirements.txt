# core (always required)
numpy>=1.24

# real satellite pipeline (Component 1) — pick one back-end
pystac-client>=0.7        # Planetary Computer STAC search
planetary-computer>=1.0   # signs asset URLs
rasterio>=1.3             # COG windowed reads, reprojection, GeoTIFF
# earthengine-api>=0.1.390  # alternative: Google Earth Engine back-end

# machine learning (python -m nagpur_uhi ml)
scikit-learn>=1.3

# optional
scipy>=1.10               # faster mask dilation / Gi* box filter
matplotlib>=3.7           # PNG maps
fastapi>=0.110            # REST API for the web front-end
uvicorn>=0.29
pandas>=2.0               # convenient tables in notebooks
